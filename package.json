{
  "name": "song-trivia-server",
  "version": "1.0.0",
  "description": "Server-authoritative multiplayer music guessing game (single room).",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --env-file-if-exists=.env server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "ingest": "node --env-file-if-exists=.env catalog/ingest.js",
    "ingest:charts": "node --env-file-if-exists=.env catalog/ingest.js charts",
    "ingest:artists": "node --env-file-if-exists=.env catalog/ingest.js artists"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "google-auth-library": "^9.14.0",
    "node-fetch": "^3.3.2",
    "pg": "^8.23.0",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  },
  "comments": {
    "optionalScaleDeps": "Loaded lazily ONLY when the matching env var is set (see DEPLOY.md): '@socket.io/redis-adapter' + 'ioredis' (REDIS_URL, multi-instance), '@sentry/node' (SENTRY_DSN, error monitoring). 'pg' is a regular dependency now — with DATABASE_URL set it persists the song catalog + global leaderboard; without it both fall back gracefully.",
    "envFileScripts": "`start` stays plain `node server.js` so it runs on any Node >=18 (hosts inject env vars themselves). The scripts that load a local .env use --env-file-if-exists, which needs Node >=22.9 — local dev only. Use `npm run dev` locally, `npm start` in production."
  }
}
